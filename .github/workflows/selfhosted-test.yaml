name: Self-hosted Runner Test

on:
  push:
    branches: [ master ]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Print Hello
        run: echo "👋 Hello from my MacBook self-hosted runner!"
